{
  "name": "local-project-console-desktop",
  "version": "1.0.0",
  "description": "Project Console desktop shell — Electron wrapper around the local Express+WS server",
  "main": "main.cjs",
  "private": true,
  "scripts": {
    "start": "electron .",
    "dist": "electron-builder --win nsis"
  },
  "build": {
    "appId": "com.localprojectconsole.app",
    "productName": "Project Console",
    "files": ["main.cjs", "preload.js", "server/**/*", "bin/**/*", "data/**/*"],
    "extraMetadata": {
      "main": "main.cjs"
    },
    "win": {
      "target": ["nsis"],
      "icon": "icon.png"
    },
    "nsis": {
      "oneClick": true,
      "perMachine": false
    },
    "mac": {
      "target": ["dmg"]
    },
    "linux": {
      "target": ["AppImage"]
    }
  },
  "devDependencies": {
    "electron": "^43.4.0",
    "electron-builder": "~26.15.3"
  }
}
